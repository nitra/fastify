{
  "name": "demo",
  "version": "1.0.0",
  "main": "src/index.js",
  "author": "<ai@nitra.ai>",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "env $(cat .env) yarn dlx nodemon src/index.js",
    "e-call": "curl --data-binary '@test/call.txt' -H 'Content-Type: application/x-www-form-urlencoded' -X POST http://localhost:8089/events/call",
    "fix": "yarn dlx standard --fix && yarn dlx prettier --write ."
  },
  "dependencies": {
    "@nitra/check-env": "^2.0.7",
    "@nitra/fastify": "^1.1.1"
  },
  "devDependencies": {
    "@nitra/eslint-config": "^1.0.22"
  },
  "eslintConfig": {
    "extends": [
      "@nitra/eslint-config/node"
    ]
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
